server:
  port: 8081
spring:
  application:
    name: Config-Server 

  cloud:
    config:
      server:
        git: 
          uri: https://github.com/Djkmr/Config-server-springboot/tree/main/demo
          clone-on-start: true 
